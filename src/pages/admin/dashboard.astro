---
import Layout from '../../layouts/Layout.astro';
---

<Layout title="Dashboard Anais Content">
  <main class="min-h-screen bg-[#F5EFEB] flex flex-col pt-24 pb-12 px-4 md:px-12 lg:px-20">
    <div class="max-w-7xl mx-auto w-full">
      
      <div class="flex flex-row justify-between items-center mb-8">
        <div>
          <h1 class="font-display text-3xl md:text-5xl font-bold text-[#1C2B3A] mb-1">Dashboard</h1>
          <p class="text-[#567C8D] font-bold uppercase tracking-[0.3em] text-[9px]">Gestión Real-Time • Anais Content</p>
        </div>
        <div class="flex items-center gap-4">
          <a href="/" target="_blank" class="hidden md:flex bg-white text-[#1C2B3A] px-4 py-2 rounded-xl font-bold uppercase tracking-widest text-[9px] hover:bg-[#C8D9E6] transition-all border border-[#C8D9E6]/30">
            Sitio Web ↗
          </a>
          <button id="logout-btn" class="text-[9px] font-black uppercase tracking-widest text-red-500/60 hover:text-red-500 transition-colors">
            Cerrar Sesión
          </button>
        </div>
      </div>
      
      <!-- Analytics Overview -->
      <div id="analytics-section" class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-10">
          <div class="bg-white p-6 rounded-[32px] shadow-sm border border-[#567C8D]/5 group hover:border-[#567C8D]/20 transition-all">
              <span class="text-[8px] font-black uppercase tracking-widest text-[#567C8D]/50 mb-2 block">Visitas</span>
              <div class="flex items-center justify-between">
                  <h3 id="stat-visits" class="text-2xl font-bold text-[#1C2B3A]">---</h3>
                  <div class="w-8 h-8 rounded-lg bg-blue-50 flex items-center justify-center text-blue-500">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"/></svg>
                  </div>
              </div>
          </div>
          <div class="bg-white p-6 rounded-[32px] shadow-sm border border-[#567C8D]/5 group hover:border-[#567C8D]/20 transition-all">
              <span class="text-[8px] font-black uppercase tracking-widest text-[#567C8D]/50 mb-2 block">Leads</span>
              <div class="flex items-center justify-between">
                  <h3 id="stat-leads" class="text-2xl font-bold text-[#1C2B3A]">---</h3>
                  <div class="w-8 h-8 rounded-lg bg-emerald-50 flex items-center justify-center text-emerald-500">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"/></svg>
                  </div>
              </div>
          </div>
          <div class="bg-white p-6 rounded-[32px] shadow-sm border border-[#567C8D]/5 group hover:border-[#567C8D]/20 transition-all">
              <span class="text-[8px] font-black uppercase tracking-widest text-[#567C8D]/50 mb-2 block">Conversión</span>
              <div class="flex items-center justify-between">
                  <h3 id="stat-conv" class="text-2xl font-bold text-[#1C2B3A]">0%</h3>
                  <div class="w-8 h-8 rounded-lg bg-amber-50 flex items-center justify-center text-amber-500">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"/></svg>
                  </div>
              </div>
          </div>
          <div class="bg-white p-6 rounded-[32px] shadow-sm border border-[#567C8D]/5 group hover:border-[#567C8D]/20 transition-all">
              <span class="text-[8px] font-black uppercase tracking-widest text-[#567C8D]/50 mb-2 block">Publicaciones</span>
              <div class="flex items-center justify-between">
                  <h3 id="stat-content" class="text-2xl font-bold text-[#1C2B3A]">---</h3>
                  <div class="w-8 h-8 rounded-lg bg-purple-50 flex items-center justify-center text-purple-500">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"/></svg>
                  </div>
              </div>
          </div>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-3 gap-6 md:gap-8">
        <!-- Columna Izquierda: Acciones Rápidas -->
        <div class="lg:col-span-1 flex flex-col gap-4">
          <h2 class="text-[#1C2B3A] font-bold text-lg mb-2">Acciones Rápidas</h2>
          
          <a href="/admin/portfolio" class="group bg-[#1C2B3A] p-6 rounded-[32px] shadow-sm hover:shadow-xl transition-all relative overflow-hidden">
            <div class="absolute top-0 right-0 w-24 h-24 bg-white/5 rounded-full -mr-12 -mt-12"></div>
            <div class="w-12 h-12 rounded-2xl bg-white/10 flex items-center justify-center mb-6 group-hover:scale-110 transition-transform">
              <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/></svg>
            </div>
            <h2 class="text-lg md:text-xl font-bold text-white mb-1 leading-tight">Portafolio</h2>
            <p class="text-[10px] text-white/50 leading-relaxed">Gestionar propiedades e imágenes.</p>
          </a>

          <a href="/admin/settings" class="group bg-white p-6 rounded-[32px] shadow-sm hover:shadow-xl transition-all border border-transparent hover:border-[#567C8D]/20">
            <div class="w-12 h-12 rounded-2xl bg-[#F5EFEB] flex items-center justify-center mb-6 group-hover:scale-110 transition-transform">
              <svg class="w-6 h-6 text-[#1C2B3A]" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"/></svg>
            </div>
            <h2 class="text-lg md:text-xl font-bold text-[#1C2B3A] mb-1 leading-tight">Configuración</h2>
            <p class="text-[10px] text-[#567C8D] leading-relaxed">Hero, Sobre Mí, SEO, Servicios, Testimonios.</p>
          </a>
        </div>

        <!-- Columna Derecha: Buzón / Últimos Mensajes -->
        <div class="lg:col-span-2 flex flex-col gap-4">
          <div class="flex justify-between items-center mb-2">
            <h2 class="text-[#1C2B3A] font-bold text-lg">Últimos Leads</h2>
            <a href="/admin/messages" class="text-[10px] font-black uppercase tracking-widest text-[#567C8D] hover:text-[#1C2B3A] transition-colors flex items-center gap-1">
              Ver Buzón Completo <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/></svg>
            </a>
          </div>
          <div class="bg-white rounded-[32px] p-2 shadow-sm border border-[#567C8D]/5 flex-1 min-h-[300px]">
            <div id="recent-messages" class="flex flex-col gap-2 p-4">
              <!-- JS Inyectará los mensajes aquí -->
              <div class="text-center py-10 text-[11px] font-bold text-[#567C8D] uppercase tracking-widest skeleton">Cargando mensajes...</div>
            </div>
          </div>
        </div>
      </div>

    </div>
  </main>
</Layout>

<script>
  import { supabase } from '../../lib/supabase';

  if (sessionStorage.getItem('admin_access') !== 'true') {
    window.location.href = '/admin';
  }
  
  document.getElementById('logout-btn')?.addEventListener('click', () => {
    sessionStorage.removeItem('admin_access');
    window.location.href = '/admin';
  });

  async function fetchStats() {
    try {
      const [
        // ── FIX Bug 3: Ya no traemos TODAS las filas de analytics.
        // Supabase no tiene COUNT DISTINCT nativo vía JS client, así que
        // pedimos solo los session_id con un límite generoso (5000).
        // Para sitios con mucho tráfico considera una DB function o vista.
        { data: visitsData },
        { count: totalLeads },
        { count: unreadLeads },
        { count: portfolio },
        { count: services },
        { data: recentMessages }
      ] = await Promise.all([
        supabase
          .from('analytics')
          .select('session_id')
          .limit(5000),                          // ← límite explícito en lugar de ilimitado
        supabase.from('messages').select('*', { count: 'exact', head: true }),
        supabase.from('messages').select('*', { count: 'exact', head: true }).eq('read', false),
        supabase.from('portfolio').select('*', { count: 'exact', head: true }),
        supabase.from('services').select('*', { count: 'exact', head: true }),
        supabase.from('messages').select('*').order('created_at', { ascending: false }).limit(5)
      ]);

      const uniqueSessions = visitsData
        ? new Set(visitsData.map((r: any) => r.session_id)).size
        : 0;
      const leadsTotal = totalLeads || 0;
      const totalItems = (portfolio || 0) + (services || 0);

      const visitsEl  = document.getElementById('stat-visits');
      const leadsEl   = document.getElementById('stat-leads');
      const convEl    = document.getElementById('stat-conv');
      const contentEl = document.getElementById('stat-content');

      if (visitsEl)  visitsEl.innerText  = uniqueSessions.toLocaleString();
      if (contentEl) contentEl.innerText = totalItems.toLocaleString();

      if (leadsEl) {
        if (unreadLeads && unreadLeads > 0) {
          leadsEl.innerHTML = `${leadsTotal} <span style="font-size:9px;background:#fffbeb;color:#f59e0b;padding:2px 8px;border-radius:9999px;margin-left:4px;font-weight:900">${unreadLeads} NEW</span>`;
        } else {
          leadsEl.innerText = leadsTotal.toLocaleString();
        }
      }

      // ── FIX Bug 6: Conversión no puede superar 100% ──────────────────────
      if (convEl && uniqueSessions > 0) {
        const rawConv = (leadsTotal / uniqueSessions) * 100;
        const conv    = Math.min(rawConv, 100).toFixed(1);   // ← cap en 100%
        convEl.innerText = `${conv}%`;
      }

      // Render recent messages
      const recentMessagesContainer = document.getElementById('recent-messages');
      if (recentMessagesContainer && recentMessages) {
        if (recentMessages.length === 0) {
          recentMessagesContainer.innerHTML = '<div class="text-center py-10 text-[11px] font-bold text-[#567C8D]/50 uppercase tracking-widest">No hay mensajes recientes</div>';
        } else {
          recentMessagesContainer.innerHTML = recentMessages.map(msg => `
            <div class="p-3 rounded-2xl bg-[#F5EFEB]/50 hover:bg-[#F5EFEB] transition-colors flex justify-between items-center group cursor-pointer" onclick="window.location.href='/admin/messages'">
              <div class="truncate pr-4 flex-1">
                <p class="text-xs font-bold text-[#1C2B3A] truncate flex items-center gap-1">${msg.name || 'Sin Nombre'} ${!msg.read ? '<span class="inline-block w-1.5 h-1.5 rounded-full bg-amber-400"></span>' : ''}</p>
                <p class="text-[10px] text-[#567C8D] truncate">${msg.message || 'Sin mensaje'}</p>
              </div>
              <div class="text-[9px] font-black uppercase tracking-widest text-[#567C8D]/50 whitespace-nowrap group-hover:text-[#567C8D]">
                ${new Date(msg.created_at).toLocaleDateString()}
              </div>
            </div>
          `).join('');
        }
      }
    } catch (e) {
      console.error('Error loading stats', e);
    }
  }

  fetchStats();
</script>